# This project is a static frontend landing page bundled with Parcel.
# It does not require any environment variables, API keys, or database credentials.
# Development and production builds are fully self-contained.
